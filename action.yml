name: "Code Review Assistant"
description: "Lists the changed files for the current pull request."
author: "Code Review Assistant"
inputs:
  github-token:
    description: "GitHub token with access to the pull request."
    required: false
runs:
  using: "node20"
  main: "dist/index.js"
branding:
  icon: "message-square"
  color: "blue"
